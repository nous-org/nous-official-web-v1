---
export interface NavLink {
    href: string;
    label: string;
    className?: string;
    target?: '_blank' | '_self' | '_parent' | '_top';
    ariaLabel?: string;
}

interface Props {
    links?: NavLink[];
    variant?: 'horizontal' | 'vertical';
    spacing?: 'sm' | 'md' | 'lg';
    className?: string;
}

// Default navigation links
const defaultLinks: NavLink[] = [
    { 
        href: '#about', 
        label: 'About',
        ariaLabel: 'Learn more about NOUS'
    },
    // { 
    //     href: '#products', 
    //     label: 'Products',
    //     ariaLabel: 'Explore our AI products'
    // },
    { 
        href: '#services', 
        label: 'Services',
        ariaLabel: 'Discover NOUS services'
    },
     { 
        href: '#pricing', 
         label: 'Pricing',
         ariaLabel: 'View pricing plans'
     },
    { 
        href: '#contact', 
        label: 'Contact',
        ariaLabel: 'Get in touch with us'
    }
];

// Default styling classes
const defaultLinkClass = 'text-white hover:text-outline transition-colors duration-300 font-medium text-sm lg:text-base uppercase tracking-[0.12em] cursor-pointer';

const {
    links = defaultLinks,
    variant = 'horizontal',
    spacing = 'md',
    className = ''
} = Astro.props;

// Dynamic classes based on props
const containerClasses = [
    'flex border border-outline/70 shadow-[0_0_28px_-12px_rgba(220,212,255,0.75)] rounded-full px-8 lg:px-10 py-4 bg-[#060114]/80 backdrop-blur-xl',
    variant === 'horizontal' ? 'flex-row' : 'flex-col',
    spacing === 'sm' ? 'gap-6 lg:gap-8' : spacing === 'md' ? 'gap-8 lg:gap-10' : 'gap-12',
    className
].filter(Boolean).join(' ');
---

<nav role="navigation" aria-label="Main navigation">
    <ul class={containerClasses}>
        {links.map((link) => (
            <li>
                <a 
                    href={link.href}
                    data-astro-reload={link.href === '/' ? true : undefined}
                    class={link.className || defaultLinkClass}
                    target={link.target || '_self'}
                    aria-label={link.ariaLabel || link.label}
                >
                    {link.label}
                </a>
            </li>
        ))}
    </ul>
</nav>
