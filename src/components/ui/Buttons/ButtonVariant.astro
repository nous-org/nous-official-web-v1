---
import ScheduleIcon from "@/components/icons/ScheduleIcon.astro"
import ArrowIcon from "@/components/icons/arrowIcon.astro"

interface Props {
  href?: string;
  text?: string;
  variant?: "primary" | "secondary" | "primaryPurple" | "secondaryTurquoise";
  className?: string;
  scheduleIcon?: boolean;
  arrowIcon?: boolean;
  strokeColor?: string;
  width?: string;
  target?: "_blank" | "_self" | "_parent" | "_top";
  ariaLabel?: string;
  rounded?: string;
  justify?: string;
}

const {
  target = "_self",
  width = "auto",
  href="contact",
  text,
  variant = "primary",
  className = "",
  scheduleIcon = false,
  arrowIcon = false,
  strokeColor = "#8458FF",
  ariaLabel = "Button",
  rounded = "full",
  justify = "center",
} = Astro.props;

const baseStyles = `px-5 py-1 font-medium rounded-${rounded} transition-colors group flex justify-${justify} items-center gap-2 w-auto md:w-${width} cursor-pointer`;

const variants = {
  primary: "bg-primary-purple text-primary-blue hover:bg-primary-purple/90",
  secondary:
    " text-primary-blue hover:bg-primary-turquoise/80 bg-primary-turquoise",
  primaryPurple:
    "bg-transparent border-2 border-primary-purple text-primary-purple hover:bg-primary-purple/40",
  secondaryTurquoise:
    "border-2 border-primary-turquoise text-primary-turquoise hover:bg-primary-turquoise/30",
};
---


<a
  href={href}
  aria-label={ariaLabel}
  target={target}
  class:list={[baseStyles, variants[variant], className]}
>
  {text}
  {
    scheduleIcon && (
      <ScheduleIcon strokeColor={strokeColor}/>
    )
  }
  {
    arrowIcon && (
      <ArrowIcon strokeColor={strokeColor}/>
    )
  }
</a>

